# Runs on EACH commit, makes sure that formatting and stuff is correct
# Tests are run on CI, so we don't need to run them here

repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v3.2.0
    hooks:
      - id: trailing-whitespace
        files: ^(src|tests)/.*\.py$
      - id: end-of-file-fixer
        files: ^(src|tests)/.*\.py$
      - id: check-yaml
        files: ^(src|tests)/.*\.py$
      - id: check-json
        files: ^(src|tests)/.*\.py$
      - id: check-added-large-files
        files: ^(src|tests)/.*\.py$
  - repo: https://github.com/PyCQA/autoflake
    rev: v2.2.1
    hooks:
      - id: autoflake
        files: ^(src|tests)/.*\.py$
        name: autoflake
        entry: autoflake
        language: python
        types: [python]
        require_serial: true
        args:
          - "--in-place"
          - "--expand-star-imports"
          - "--remove-duplicate-keys"
          - "--remove-unused-variables"
  - repo: local
    hooks:
      - id: black
        files: ^(src|tests)/.*\.py$
        name: black
        language: system
        entry: pdm run black .
        pass_filenames: false
        always_run: true

      - id: mypy
        files: ^(src|tests)/.*\.py$
        name: mypy
        language: system
        entry: pdm run mypy .
        args:
          - "--ignore-missing-imports"
          - "--install-types"
          - "--explicit-package-bases"
        pass_filenames: false
        always_run: true

      - id: pytest
        files: ^(src|tests)/.*\.py$
        name: tests
        entry: pdm run pytest --maxfail=1
        language: system
        pass_filenames: false
        always_run: true

      - id: sphinx
        files: ^(src|tests)/.*\.py$
        name: documentation
        entry: pdm run sphinx-build -b html -a sphinx/source docs/sphinx
        language: system
        pass_filenames: false
        always_run: true
